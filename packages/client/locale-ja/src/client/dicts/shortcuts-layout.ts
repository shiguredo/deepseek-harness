/** Japanese dictionary for the `shortcuts.layout` namespace (client/ui-layout/src/client/shortcut-locales.ts). */
import type { LocaleDictOf } from '@deepseek-ai/dsh-client-ui-slots'
// Type-only: loads the `shortcuts.layout` key union declared by @deepseek-ai/dsh-client-ui-layout.
import type {} from '@deepseek-ai/dsh-client-ui-layout/client'

/** `shortcuts.layout` copy in Japanese, complete against the owner's key union. */
export const ja: LocaleDictOf<'shortcuts.layout'> = {
  'toggle': '左サイドバーを開閉',
}
